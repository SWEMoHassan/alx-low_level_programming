#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: This function uses printf to print a specific message
 * with proper grammar, but the outcome is a piece of art,
 * followed by a new line. The function returns 0.
 *
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
